import SwiftUI

// This view is no longer used - can be safely removed from project
struct EditorPreferencesView: View {
    var body: some View {
        Text("Editor Preferences have been removed")
            .navigationTitle("Editor Preferences")
    }
}

#Preview {
    EditorPreferencesView()
}